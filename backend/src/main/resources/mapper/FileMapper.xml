<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.filesystem.mapper.FileMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.filesystem.entity.SysFile">
        <id column="ID" property="id" />
        <result column="FILE_NAME" property="fileName" />
        <result column="FILE_SIZE" property="fileSize" />
        <result column="MIME_TYPE" property="mimeType" />
        <result column="FILE_CONTENT" property="fileContent" />
        <result column="UPLOADED_BY" property="uploadedBy" />
        <result column="CREATED_AT" property="createdAt" />
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        ID, FILE_NAME, FILE_SIZE, MIME_TYPE, UPLOADED_BY, CREATED_AT
    </sql>

    <!-- 分页查询文件列表 -->
    <select id="selectFilePage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM SYS_FILES
        <where>
            <if test="uploadedBy != null">
                AND UPLOADED_BY = #{uploadedBy}
            </if>
        </where>
        ORDER BY CREATED_AT DESC
    </select>

</mapper>
