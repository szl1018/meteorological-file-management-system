<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.filesystem.mapper.LogMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.filesystem.entity.OperationLog">
        <id column="ID" property="id" />
        <result column="USER_ID" property="userId" />
        <result column="USERNAME" property="username" />
        <result column="ACTION" property="action" />
        <result column="FILE_ID" property="fileId" />
        <result column="FILE_NAME" property="fileName" />
        <result column="IP_ADDRESS" property="ipAddress" />
        <result column="CREATED_AT" property="createdAt" />
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        ID, USER_ID, USERNAME, ACTION, FILE_ID, FILE_NAME, IP_ADDRESS, CREATED_AT
    </sql>

    <!-- 分页查询操作日志 -->
    <select id="selectLogPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM SYS_OPERATION_LOGS
        <where>
            <if test="userId != null">
                AND USER_ID = #{userId}
            </if>
            <if test="action != null and action != ''">
                AND ACTION = #{action}
            </if>
            <if test="startDate != null and startDate != ''">
                AND TO_CHAR(CREATED_AT, 'YYYY-MM-DD') &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND TO_CHAR(CREATED_AT, 'YYYY-MM-DD') &lt;= #{endDate}
            </if>
        </where>
        ORDER BY CREATED_AT DESC
    </select>

</mapper>
